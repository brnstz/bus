language: go
go:
    - 1.6

services:
    - postgresql
    - redis-server

env:
    - BUS_API_ADDR=":8000" BUS_DB_ADDR="localhost:5432" BUS_DB_USER="postgres" BUS_REDIS_ADDR="localhost:6379" 

script: ./automation/ci.sh
